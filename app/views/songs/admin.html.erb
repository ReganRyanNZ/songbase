<%= render 'flash' %>

<%= react_component('AdminDeviceBooks', {}) %>

<%= react_component(
  'AdminSongList',
  { songs: @songs.map(&:admin_entry) },
  settings: {
    languages: Song.languages
  })
%>